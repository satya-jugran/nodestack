import { BaseAuthStore } from './BaseAuthStore';

/**
 * In-memory authentication store.
 * Ideal for Node.js scripts, server-side rendering (SSR), and testing environments.
 */
export class MemoryAuthStore extends BaseAuthStore {
  // Inherits default in-memory behavior of BaseAuthStore
}
